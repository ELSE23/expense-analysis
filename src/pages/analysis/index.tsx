import React from 'react';
import { Card, Divider } from 'antd';
import styles from './style.module.scss';

const Doc = () => {
  return <Card title="文档" className={styles.container}></Card>;
};

export default Doc;
